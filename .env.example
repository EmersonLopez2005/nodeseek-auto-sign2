# NodeSeek 多站点自动签到脚本环境变量配置示例

# ========== 基础配置 ==========
# 随机数参数（可选）
NS_RANDOM=true

# ========== NodeSeek 配置 ==========
# 账号密码配置（格式：用户名1&密码1&用户名2&密码2）
# 用于Cookie失效时自动登录，每个账号会独立保存Cookie文件
NS_COOKIE=你的NodeSeek用户名1&你的NodeSeek密码1&你的NodeSeek用户名2&你的NodeSeek密码2

# ========== DeepFlood 配置 ==========
# 账号密码配置（格式：用户名1&密码1&用户名2&密码2）
# 用于Cookie失效时自动登录，每个账号会独立保存Cookie文件
DF_COOKIE=你的DeepFlood用户名1&你的DeepFlood密码1&你的DeepFlood用户名2&你的DeepFlood密码2

# ========== 验证码服务配置 ==========
# CloudFreed 自建验证码服务（必须配置）
# 1. 首先部署 CloudFreed 服务：
#    docker run -itd --name cloudflyer -p 3000:3000 --restart unless-stopped jackzzs/cloudflyer -K 你的客户端密钥 -H 0.0.0.0
# 2. 然后配置以下参数：
CLOUDFREED_API_KEY=你的CloudFreed客户端密钥
# CloudFreed 服务地址（修改为您的服务器IP和端口，注意：在Docker容器内使用宿主机IP）
CLOUDFREED_BASE_URL=http://您的服务器IP:3000

# ========== 通知服务配置 ==========
# Telegram 通知配置（可选）
TG_BOT_TOKEN=你的Telegram Bot Token
TG_USER_ID=你的Telegram用户ID

# ========== 其他配置 ==========
# Docker环境标识（Docker部署时自动设置）
IN_DOCKER=false
